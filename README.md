This is a Vite + React scaffold that mirrors the original Kaizenbank site.

Notes:
- The app references existing assets in `../Kaizenbank/assets` to preserve styles and scripts.
- Contact form posts to `/Kaizenbank/sendemail.php` to keep backend behavior unchanged.

Commands:
- `npm install` then `npm run dev` to start development.
# kaizen-new-website
